services:
  - type: web
    name: todo-app
    runtime: node
    buildCommand: npm run install:all && npm run build
    startCommand: npm start
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: todo-db
          property: connectionString
      - key: NODE_ENV
        value: production

databases:
  - name: todo-db
    databaseName: todo
    user: todo
